// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  schemas  = ["user", "space", "commodity"]
}

// 主 schema 文件
// 模型定义已拆分到 models 目录下的各个文件中
